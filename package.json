{
  "name": "fingerprint",
  "version": "1.0.0",
  "description": "Collaborative fingerprinting experiment",
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && npx wrangler pages deploy ./dist"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
